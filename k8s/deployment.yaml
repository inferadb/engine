apiVersion: apps/v1
kind: Deployment
metadata:
  name: inferadb
  labels:
    app: inferadb
    component: server
    version: v1.0.0
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: inferadb
      component: server
  template:
    metadata:
      labels:
        app: inferadb
        component: server
        version: v1.0.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: inferadb
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault

      # Init container to wait for dependencies
      initContainers:
        - name: wait-for-fdb
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for FoundationDB to be ready..."
              until nc -z -v -w30 foundationdb-cluster 4500; do
                echo "Waiting for FoundationDB..."
                sleep 5
              done
              echo "FoundationDB is ready!"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true

      containers:
        - name: inferadb
          image: inferadb:latest
          imagePullPolicy: IfNotPresent

          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: grpc
              containerPort: 8081
              protocol: TCP
            - name: metrics
              containerPort: 8080
              protocol: TCP

          env:
            - name: RUST_LOG
              value: "info"
            - name: RUST_BACKTRACE
              value: "1"
            - name: INFERA__SERVER__HOST
              value: "0.0.0.0"
            - name: INFERA__SERVER__PORT
              value: "8080"
            - name: INFERA__OBSERVABILITY__METRICS_ENABLED
              value: "true"
            - name: INFERA__OBSERVABILITY__TRACING_ENABLED
              value: "true"

          # Load config from ConfigMap
          envFrom:
            - configMapRef:
                name: inferadb-config

            # Load secrets from Secret
            - secretRef:
                name: inferadb-secrets

          # Volume mounts
          volumeMounts:
            - name: config
              mountPath: /etc/inferadb
              readOnly: true
            - name: fdb-cluster-file
              mountPath: /etc/foundationdb
              readOnly: true
            - name: tmp
              mountPath: /tmp

          # Resource limits
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 2000m
              memory: 2Gi

          # Health checks
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            successThreshold: 1
            failureThreshold: 3

          startupProbe:
            httpGet:
              path: /health/startup
              port: http
            initialDelaySeconds: 0
            periodSeconds: 5
            timeoutSeconds: 3
            successThreshold: 1
            failureThreshold: 30

          # Security context
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532

      # Volumes
      volumes:
        - name: config
          configMap:
            name: inferadb-config
            items:
              - key: config.yaml
                path: config.yaml

        - name: fdb-cluster-file
          configMap:
            name: foundationdb-cluster-file
            items:
              - key: fdb.cluster
                path: fdb.cluster

        - name: tmp
          emptyDir: {}

      # Affinity rules for better distribution
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - inferadb
                topologyKey: kubernetes.io/hostname

      # Topology spread for better availability
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: inferadb

      # Termination grace period for graceful shutdown
      terminationGracePeriodSeconds: 30
